<resources>


    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/default_background_colour</item>
        <!--<item name="android:colorBackground">@color/default_background_colour</item>-->
        <item name="android:windowActionBar">false</item>

        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.TransparentStatus">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@color/colorPrimaryDarkTransparent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Widget.Icon" parent="@android:style/Widget" />

    <style name="Widget.Icon.Dialog">
        <item name="android:background">@drawable/dialog_bg</item>
        <item name="android:elevation">@dimen/dialog_elevation</item>
    </style>

    <style name="Widget.SocialButton" parent="Widget.MaterialComponents.Button">
        <item name="android:textAlignment">textStart</item>
        <item name="iconSize">@dimen/button_icon_size</item>
        <item name="android:paddingStart">@dimen/spacing_tiny</item>
        <item name="android:paddingEnd">@dimen/spacing_micro</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textAppearance">@style/TextAppearance.MaterialComponents.Button</item>
    </style>

    <color name="default_background_colour">#ffffff</color>

</resources>